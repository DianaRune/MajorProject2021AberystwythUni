import java.util.Random;

public class Seeds {
    int seed = 0;

            //Random.seed(seed);

            //int
}
/*
/ Select random seed, or choose one specifically
int seed = 69;

// 'Seed' the pseudorandom number generator
Random.seed(seed);

// Get some random numbers
int key = Random.nextInt() % 12;
int tempo = Random.nextInt() % 200;
// ...
// And now we have random parameters to set up the song.

// And this original seed can be used to reset the random generator.
Random.seed(seed);

// And now we can generate exactly the same song reliably while still using the method Random.nextInt()

assert(Random.nextInt() % 12 == key);

assert(Random.nextInt() % 200 == tempo);
 */